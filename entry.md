# Входное тестирование:

Всего вопросов: 50

- Computer Science: 8 вопроса
- Java Core: 13 вопроса
- Git: 4 вопроса
- SQL: 4 вопроса
- Вопросы по программе курса: 31 вопрос

---

# Блок Computer Science

### Вопрос 1:

Сложность поиска элемента в хэш-таблице в среднем случае равна:

- [ ] `O(log n)`
- [x] `O(1)`
- [ ] `O(n)`
- [ ] `O(n log n)`

---

### Вопрос 2:

Какое основное отличие стека от очереди?

- [x] Стек работает по принципу LIFO, очередь — FIFO
- [ ] Стек работает по принципу FIFO, очередь — LIFO
- [ ] В стеке можно удалять элементы только из начала, в очереди — из конца
- [ ] Стек хранит только числа, очередь — строки

---

### Вопрос 3:

Что такое **Big O** нотация?

- [ ] Система кодировки символов
- [ ] Мера использования оперативной памяти
- [x] Оценка асимптотической сложности алгоритма
- [ ] Способ оптимизации базы данных

---

### Вопрос 4:

Какой протокол используется для безопасной передачи данных в интернете?

- [ ] FTP
- [ ] HTTP
- [x] HTTPS
- [ ] POP3

---

### Вопрос 5:

Какое утверждение верно про рекурсию?

- [ ] Рекурсия всегда работает быстрее, чем цикл
- [ ] Рекурсия требует меньше памяти, чем итерации
- [ ] Рекурсия используется только в сортировках
- [x] Рекурсия - это вызов функцией самой себя

---

### Вопрос 6:

Что из перечисленного является примером NoSQL базы данных?

- [ ] Postgres
- [x] MongoDB
- [ ] MySQL
- [ ] MariaDB

---

### Вопрос 7:

Какой стандарт кодировки символов поддерживает большинство языков мира?

- [ ] ASCII
- [ ] ISO-8859-1
- [x] Unicode (UTF-8)
- [ ] Base64

---

### Вопрос 8:

Что такое **deadlock** в многопоточном программировании?

- [ ] Поток завершился с ошибкой
- [ ] Поток зациклился в бесконечном цикле
- [x] Несколько потоков ждут друг друга, и работа не продолжается
- [ ] Потоки используют слишком много памяти

---

# Блок Java Core

### Вопрос 9:

Сколько примитивных типов данных существует в Java?

- [ ] 6
- [ ] 7
- [x] 8
- [ ] 9

### Вопрос 10:

Какой результат работы кода:

`java
System.out.println(5 + "5");
`

- [x] "55"
- [ ] 10
- [ ] "10"
- [ ] Ошибка компиляции

---

### Вопрос 11:

Какой модификатор доступа позволяет видеть поле/метод класса только внутри пакета?

- [ ] public
- [ ] private
- [x] package-private (по умолчанию)
- [ ] protected

---

### Вопрос 12:

Что делает ключевое слово `synchronized`?

- [x] Ограничивает доступ к блоку/методу одному потоку одновременно
- [ ] Запрещает наследование метода
- [ ] Создаёт новый поток
- [ ] Отмечает метод как абстрактный

### Вопрос 13:

Какой результат работы кода:

`java
String s = "Hello";
s.concat(" World");
System.out.println(s);
`

- [ ] Hello World
- [ ] World
- [x] Hello
- [ ] Ошибка компиляции

---

### Вопрос 14:

Какая из коллекций не поддерживает индексацию по числовому индексу?

- [ ] `ArrayList`
- [x] `HashSet`
- [ ] `LinkedList`
- [ ] `Vector`

---

### Вопрос 15:

Какой модификатор запрещает наследование класса?

- [ ] `abstract`
- [ ] `static`
- [x] `final`
- [ ] `private`

---

### Вопрос 16:

Что такое "автоупаковка" (autoboxing) в Java?

- [ ] Преобразование объекта-обёртки в примитивный тип
- [x] Автоматическое преобразование примитивного типа в объект-обёртку
- [ ] Автоматическое освобождение памяти от неиспользуемых объектов
- [ ] Преобразование строки в число

---

### Вопрос 17:

Что будет напечатано в результате выполнения этого кода?

`java
Integer a = 100;
Integer b = 100;
Integer x = 128;
Integer y = 128;
System.out.println(a == b);
System.out.println(x == y);
`

- [ ] `true` / `true`
- [ ] `false` / `false`
- [x] `true` / `false`
- [ ] `false` / `true`

---

### Вопрос 18:

Какой класс является суперклассом для всех исключений в Java?

- [ ] `Exception`
- [x] `Throwable`
- [ ] `RuntimeException`
- [ ] `Error`

---

### Вопрос 19:

В каком из списков все интерфейсы являются функциональными и могут быть использованы в лямбда-выражениях?

- [ ] `Runnable, Cloneable, AutoCloseable`
- [x] `Runnable, Comparator, Function`
- [ ] `List, Map`
- [ ] `Object, String`

---

### Вопрос 20:

Какой класс используется для работы с байтовыми потоками?

- [ ] `FileReader`
- [ ] `BufferedWriter`
- [x] `FileInputStream` / `FileOutputStream`
- [ ] `PrintWriter`

---

### Вопрос 21:

Что произойдёт при компиляции следующего кода?

`java
List<String> list = new ArrayList<>();
List<Object> objects = list; // ?
`

- [x] Код не скомпилируется из-за инвариантности Generics
- [ ] Код успешно скомпилируется и выполнится
- [ ] Код скомпилируется только если использовать `List<?>` вместо `List<Object>`
- [ ] Будет предупреждение, но код скомпилируется

---

# Блок GIT

### Вопрос 22:

Что делает команда `git stash`?

- [ ] Безвозвратно удаляет изменения
- [ ] Переносит изменения в новую ветку
- [x] Сохраняет незакоммиченные изменения во временное хранилище
- [ ] Отменяет последний коммит

---

### Вопрос 23:

Для чего используется команда `git cherry-pick <commit>`?

- [ ] Удаляет указанный коммит из истории
- [ ] Копирует все коммиты из другой ветки
- [x] Применяет изменения конкретного коммита в текущую ветку
- [ ] Отменяет изменения последнего коммита

---

### Вопрос 24:

Чем отличается команда `git merge` от `git rebase`?

- [ ] Оба создают новый коммит, объединяющий изменения
- [x] merge сохраняет историю веток, а rebase переписывает её
- [ ] merge всегда приводит к конфликту, а rebase — нет
- [ ] Разницы нет, это полные аналоги

---

### Вопрос 25:

В чем отличие команд `git fetch` отт `git pull?`

- [x] fetch только скачивает изменения без слияния
- [ ] fetch скачивает и объединяет изменения
- [ ] fetch удаляет старые ветки
- [ ] Разницы нет

---

# Блок SQL

### Вопрос 26:

Что делает оператор `GROUP BY` в SQL?

- [x] Группирует строки по значению одного или нескольких столбцов
- [ ] Сортирует строки по значению одного или нескольких столбцов
- [ ] Ограничивает количество возвращаемых строк
- [ ] Объединяет несколько таблиц

---

### Вопрос 27:

Какой тип соединения в SQL вернёт все строки из левой таблицы и совпадающие строки из правой?

- [ ] INNER JOIN
- [x] LEFT JOIN
- [ ] RIGHT JOIN
- [ ] FULL JOIN

---

### Вопрос 28:

Что делает индекс в базе данных?

- [ ] Ускоряет выполнение всех запросов
- [x] Ускоряет выборку, но может замедлять операции вставки/обновления
- [ ] Автоматически нормализует таблицу
- [ ] Уменьшает размер базы данных

---

### Вопрос 29:

Что произойдет при выполнении запроса?

```sql
SELECT COUNT(*)
FROM employees
WHERE department_id IS NULL;
```

- [ ] Запрос вернет количество всех сотрудников
- [ ] Запрос вернет количество сотрудников, у которых department_id = 0
- [x] Запрос вернет количество сотрудников без департамента (NULL)
- [ ] Ошибка, COUNT нельзя использовать с NULL

# Блок вопросов по курсу

### Вопрос 30:

Какой инструмент сборки чаще всего используется в проектах Java?

- [ ] Ant
- [x] Maven/Gradle
- [ ] Makefile
- [ ] CMake

---

### Вопрос 31:

Какой файл конфигурации используется в Maven для описания зависимостей и плагинов?

- [ ] build.gradle
- [ ] application.yml
- [x] pom.xml
- [ ] settings.gradle

---

### Вопрос 32:

Какой объект в Spring описывает создаваемый компонент?

- [ ] Service
- [ ] Repository
- [x] Bean
- [ ] Controller

---

### Вопрос 33:

Какую аннотацию используют для внедрения зависимости в Spring?

- [ ] `@Bean`
- [ ] `@Component`
- [x] `@Autowired`
- [ ] `@Primary`

---

### Вопрос 34:

Какой паттерн проектирования лежит в основе Spring IoC?

- [ ] Observer
- [x] Dependency Injection
- [ ] Factory Method
- [ ] Adapter

---

### Вопрос 35:

Для чего используют `application.yml`/`application.properties` в Spring Boot?

- [ ] Для описания зависимостей проекта
- [x] Для хранения конфигурации приложения
- [ ] Для хранения логов
- [ ] Для тестирования

---

### Вопрос 36:

Что делает аннотация `@Value("${property.name}")`?

- [ ] Подключает бины
- [x] Подставляет значение из файла конфигурации
- [ ] Объявляет новый профиль
- [ ] Настраивает Actuator

---

### Вопрос 37:

Как в Spring Boot настроить подключение к БД??

- [ ] Только вручную через JDBC
- [ ] Только с Hibernate
- [x] Через свойства `spring.datasource....`
- [ ] Через Lombok

---

### Вопрос 38:

Что делает аннотация `@Data` библиотеки Lombok?

- [ ] Создаёт REST-контроллер
- [ ] Включает Actuator
- [x] Генерирует геттеры, сеттеры, методы `equals`, `hashCode`, `toString`
- [ ] Настраивает БД

---

### Вопрос 39:

Что такое `JpaRepository` в Spring Data JPA?

- [ ] Класс для запуска приложения
- [x] Интерфейс для стандартных CRUD-операций с сущностями
- [ ] Модуль для логирования
- [ ] Библиотека для написания тестов

---

### Вопрос 40:

Какой подход применяет Hibernate для связи объектов и таблиц?

- [ ] JDBC-пулы
- [x] ORM (Object-Relational Mapping)
- [ ] DAO-шаблоны
- [ ] Кэширование

---

### Вопрос 41:

Что из перечисленного является преимуществом использования Spring Boot Starter?

- [ ] Автоматически пишет тесты за разработчика
- [ ] Обеспечивает hot reload приложения
- [x] Упрощает подключение зависимостей и содержит автоконфигурацию
- [ ] Хранит настройки безопасности

---

### Вопрос 42:

Для чего используется класс `ObjectMapper` в Jackson?

- [ ] Для управления транзакциями
- [x] Для сериализации и десериализации JSON
- [ ] Для маппинга SQL-запросов
- [ ] Для логирования ошибок

---

### Вопрос 43:

Что из перечисленного верно про транзакцию в базе данных?

- [ ] Это всегда операция чтения
- [x] Это единый набор операций, выполняющийся полностью или откатывающийся
- [ ] Это всегда операция записи
- [ ] Это SQL-запрос без WHERE

---

### Вопрос 44:

Что делает аннотация `@Entity` в JPA?

- [ ] Помечает класс как сервис
- [ ] Подключает репозиторий
- [x] Помечает класс как сущность для базы данных
- [ ] Создает REST-контроллер

---

### Вопрос 45:

Чем отличается проверяемое исключение от непроверяемого исключения в Java?

- [ ] Проверяемое исключение наследуется от `Error`
- [ ] Непроверяемое исключение всегда логируется
- [x] Проверяемое исключение нужно явно обрабатывать или объявлять в сигнатуре метода
- [ ] Непроверяемое можно обрабатывать только через `try/catch`

### Вопрос 46:

Какая аннотация в Spring Boot позволяет централизованно обрабатывать исключения для
REST-контроллеров?

- [ ] `@ExceptionProcessor`
- [ ] `@Actuator`
- [x] `@RestControllerAdvice`
- [ ] `@Cacheable`

---

### Вопрос 47:

Что такое DTO и зачем его используют?

- [x] Объект для передачи данных между слоями без логики
- [ ] Сущность базы данных
- [ ] Сервис для обработки REST-запросов
- [ ] Обёртка для транзакций

---

### Вопрос 48:

Какая аннотация используется для валидации параметров в Spring?

- [ ] `@Valid`
- [ ] `@Validated`
- [ ] `@NotNull`
- [x] Все перечисленные

---

### Вопрос 49:

Какой инструмент в Spring Boot позволяет мониторить приложение (метрики, health-check)?

- [ ] Hibernate
- [ ] Swagger
- [x] Actuator
- [ ] Lombok

---

### Вопрос 50:

Что делает аннотация `@SpringBootApplication`?

- [ ] Запускает встроенный сервер базы данных
- [ ] Определяет REST-контроллер
- [x] Объединяет конфигурации и запускает приложение Spring Boot
- [ ] Создаёт файл `application.properties` автоматически  



























